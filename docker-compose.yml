services:
  backend:
    image: ghcr.io/agentgg00/server-dashboard:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend