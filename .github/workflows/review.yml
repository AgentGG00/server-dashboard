name: Review

on:
  pull_request:
    branches:
      - main

permissions:
  pull-requests: write
  actions: write

jobs:
  review:
    uses: AgentGG00/workflows/.github/workflows/review.yml@main
    secrets: inherit
